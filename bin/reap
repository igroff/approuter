#! /usr/bin/env bash

MY_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source ${MY_DIR}/environment

# check age of active instances
# if active instances < 5 minutes old
#   do nothing
# else
#   for each non active instance
#      for each running port
#        stop perp service
#        deactivate perp service
#        delete perp configuraiton
#      delete instance directory 

