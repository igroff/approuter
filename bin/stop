#! /usr/bin/env bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source $DIR/environment
ACTION=$1
if [ "${ACTION}x" == "x" ]; then
  # we do expect to own the machine, so this is
  # reasonable
  killall perpd
else
  perpctl D ${ACTION}
fi
